namespace AlertSense.PingPong.ServiceModel.Enums
{
    /// <summary>
    /// Code relies on Side One being set to 0 and Side Two being set to 1 
    /// Values are used to index into an array
    /// 
    /// </summary>
    public enum Side
    {
        One=0,
        Two=1,
        None=2
    }
}